{
  "name": "stronghold-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "gen:types": "cd ../backend && uv run python export_openapi.py && cd ../frontend && openapi-typescript ../backend/openapi.json -o src/lib/api-types.ts && node scripts/gen-validators.mjs",
    "check:types-fresh": "npm run gen:types && git diff --exit-code src/lib/api-types.ts src/lib/validators.ts"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.8",
    "@sveltejs/kit": "^2.15.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "openapi-typescript": "^7.5.0",
    "svelte": "^5.15.0",
    "svelte-check": "^4.1.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "valibot": "^1.4.2"
  }
}
